@model IEnumerable<ColegioTerciario.DAL.Models.Hora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HORA_INICIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA_FIN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA_NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA_TURNO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_INICIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_FIN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_TURNO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
